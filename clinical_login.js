import {auth,functions} from "./common.js";import {httpsCallable} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-functions.js";import {signInWithCustomToken} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";const $=id=>document.getElementById(id);$("loginForm").onsubmit=async e=>{e.preventDefault();const button=e.submitter,status=$("loginStatus"),id=$("staffId").value.trim();button.disabled=true;status.textContent="ログインを確認中…";try{const result=await httpsCallable(functions,"authenticateClinical")({studentNumber:id,password:$("password").value});await signInWithCustomToken(auth,result.data.token);localStorage.setItem("clinicalStaffId",id);location.replace("clinical_workspace.html")}catch(error){console.error(error);status.textContent=error.code==="functions/permission-denied"?"この職員IDにはClinical権限が設定されていません。管理者に確認してください。":"ログインできませんでした。職員IDとパスワードを確認してください。"}finally{button.disabled=false}};

import {auth} from "./common.js";import {getIdTokenResult,signOut,onAuthStateChanged} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";const label={administrator:"Clinical管理者",doctor:"医師",nurse:"看護師",pharmacist:"薬剤師",pt:"理学療法士（PT）",ot:"作業療法士（OT）",st:"言語聴覚士（ST）",clerk:"医事・受付",auditor:"監査担当"};const role=document.getElementById("staffRole"),errorBox=document.getElementById("clinicalError");function showError(message){errorBox.textContent=message;errorBox.style.display="block";role.textContent="Clinicalログインを確認できませんでした"}onAuthStateChanged(auth,async user=>{if(!user){showError("ログイン情報を確認できません。下のボタンからもう一度ログインしてください。");return}try{const token=await getIdTokenResult(user);if(token.claims.clinical!==true){showError("このアカウントにはClinical権限が反映されていません。管理者側で職員IDを登録後、もう一度ログインしてください。");return}role.textContent=`${label[token.claims.clinicalRole]||"Clinical職員"} ・ ${token.claims.clinicalHospitalId}`}catch(error){console.error(error);showError("権限情報を読み込めませんでした。下のボタンから再ログインしてください。")}});document.querySelector("a.btn").onclick=async e=>{e.preventDefault();await signOut(auth);localStorage.removeItem("clinicalStaffId");location.replace("clinical_login.html")};

import {functions,auth,setupTheme,initializePage,loadProfileImage,loadUserName,setupAdminTab} from "./common.js";
import {httpsCallable} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-functions.js";
import {getIdTokenResult} from "https://www.gstatic.com/firebasejs/12.2.1/firebase-auth.js";
const $=id=>document.getElementById(id);setupTheme($("themeButton"));await initializePage([setupAdminTab(),loadUserName($("userName")),loadProfileImage($("topProfileImage"))]);
const claims=auth.currentUser?await getIdTokenResult(auth.currentUser):null;
if(!(claims?.claims?.clinical===true&&claims.claims.clinicalRole==="administrator")){document.querySelector("main").innerHTML='<section class="card setting-card"><h2>病院管理は病院管理者専用です</h2><p>病院を登録したアカウントでClinicalへログインしてください。</p><p><a class="btn btn-primary" href="clinical_entry.html">Clinical入口へ戻る</a></p></section>';throw new Error("Clinical hospital administrator only")}
const hospitalId=String(claims.claims.clinicalHospitalId||"");$("hospitalId").value=hospitalId;$("hospitalLabel").textContent=`病院ID：${hospitalId}`;
$("staffForm").onsubmit=async e=>{e.preventDefault();const button=e.submitter,status=$("staffStatus");button.disabled=true;status.textContent="職員を登録中…";try{await httpsCallable(functions,"configureClinicalStaff")({hospitalId,staffId:$("staffUid").value.trim(),role:$("staffRole").value,active:true});status.textContent="職員と職種を登録しました。対象職員はClinicalへログインできるようになります。"}catch(error){console.error(error);status.textContent=error.code==="functions/not-found"?"指定した職員IDのCareMateアカウントが見つかりません。":"職員を登録できませんでした。職員IDを確認してください。"}finally{button.disabled=false}};

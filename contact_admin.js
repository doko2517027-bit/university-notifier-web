import{db,studentNumber,setupTheme,initializePage}from"./common.js";import{collection,onSnapshot,query,orderBy,doc,updateDoc,serverTimestamp}from"https://www.gstatic.com/firebasejs/12.2.1/firebase-firestore.js";setupTheme(document.getElementById("themeButton"));document.getElementById("backButton").onclick=()=>history.back();const list=document.getElementById("contactList");if(studentNumber!=="2510044"){list.innerHTML='<div class="card">この画面を利用できません。</div>'}else{await initializePage([new Promise(resolve=>onSnapshot(query(collection(db,"contacts"),orderBy("createdAt","desc")),snap=>{list.innerHTML=snap.docs.map(d=>{const x=d.data();return `<article class="card setting-card"><small>${x.createdAt?.toDate?.().toLocaleString("ja-JP")||"送信直後"}・${x.studentNumber}</small><h3>${escapeHtml(x.category)}</h3><p>${escapeHtml(x.message).replace(/\n/g,"<br>")}</p><button class="btn btn-secondary contact-done" data-id="${d.id}" ${x.status==="done"?"disabled":""}>${x.status==="done"?"対応済み":"対応済みにする"}</button></article>`}).join("")||'<div class="card">お問い合わせはありません。</div>';resolve()}))])}document.addEventListener("click",async e=>{const b=e.target.closest(".contact-done");if(!b)return;await updateDoc(doc(db,"contacts",b.dataset.id),{status:"done",handledAt:serverTimestamp(),handledBy:studentNumber})});function escapeHtml(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
